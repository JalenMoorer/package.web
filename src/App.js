import React from 'react';
import PackageJSONContainer from './Containers/PackageJSON/PackageJSONContainer';

const App = (props) => (
  <div>
    <PackageJSONContainer />
  </div>

);

export default App;
